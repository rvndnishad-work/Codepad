{
  "react": {
    "answer": "## Building a Canvas Drawing Board in React\n\n**Mental model:** Get the canvas 2D context once via a ref. Pointer events drive a tiny state machine: `pointerdown` starts a stroke (`beginPath` + `moveTo`), `pointermove` while drawing extends it (`lineTo` + `stroke`), `pointerup` ends it. Brush colour/size are just context properties set before stroking. Convert client coords to canvas coords with `getBoundingClientRect`.\n\n### Step 1 — Grab the context\n```jsx\nconst canvasRef = useRef(null);\nconst ctx = () => canvasRef.current.getContext('2d');\n```\n\n### Step 2 — Start / draw / end\n```jsx\nconst start = (e) => { drawing.current = true; const c = ctx(); c.beginPath(); c.moveTo(...pos(e)); };\nconst move = (e) => { if (!drawing.current) return; const c = ctx(); c.lineTo(...pos(e)); c.stroke(); };\nconst end = () => { drawing.current = false; };\n```\n\n### Step 3 — Map client → canvas coords\n```jsx\nconst pos = (e) => { const r = canvasRef.current.getBoundingClientRect(); return [e.clientX - r.left, e.clientY - r.top]; };\n```\n\n### Step 4 — Brush settings + clear\nSet `strokeStyle`/`lineWidth`/`lineCap` before drawing; clear with `clearRect`.\n\n```jsx\nconst c = ctx(); c.strokeStyle = color; c.lineWidth = size; c.lineCap = 'round';\n```\n\n**Edge cases**\n\n| Case | Handling |\n| :-- | :-- |\n| Pointer leaves canvas | End the stroke on `pointerleave` too. |\n| HiDPI blur | Scale the backing store by `devicePixelRatio`. |\n| Touch | `pointer*` events cover mouse + touch uniformly. |\n\n**Interview tip:** Using Pointer Events (not separate mouse/touch handlers) and converting coordinates via `getBoundingClientRect` are the two correctness details — and `lineCap: 'round'` is what stops strokes looking jagged.\n",
    "files": {
      "/App.js": "import React from 'react';\nimport DrawingBoard from './src/DrawingBoard';\n\nexport default function App() {\n  return (\n    <div style={{ fontFamily: 'sans-serif', padding: 20 }}>\n      <h1>Drawing Board Demo</h1>\n      <DrawingBoard />\n    </div>\n  );\n}\n",
      "/src/DrawingBoard.js": "import React, { useRef, useState } from 'react';\n\nconst COLORS = ['#111827', '#dc2626', '#16a34a', '#2563eb', '#d97706'];\n\nexport default function DrawingBoard() {\n  const canvasRef = useRef(null);\n  const drawing = useRef(false);\n  const [color, setColor] = useState('#111827');\n  const [size, setSize] = useState(4);\n\n  const pos = (e) => {\n    const r = canvasRef.current.getBoundingClientRect();\n    return [e.clientX - r.left, e.clientY - r.top];\n  };\n\n  const start = (e) => {\n    drawing.current = true;\n    const c = canvasRef.current.getContext('2d');\n    const [x, y] = pos(e);\n    c.beginPath();\n    c.moveTo(x, y);\n  };\n\n  const move = (e) => {\n    if (!drawing.current) return;\n    const c = canvasRef.current.getContext('2d');\n    c.strokeStyle = color;\n    c.lineWidth = size;\n    c.lineCap = 'round';\n    c.lineJoin = 'round';\n    const [x, y] = pos(e);\n    c.lineTo(x, y);\n    c.stroke();\n  };\n\n  const end = () => {\n    drawing.current = false;\n  };\n\n  const clear = () => {\n    const cv = canvasRef.current;\n    cv.getContext('2d').clearRect(0, 0, cv.width, cv.height);\n  };\n\n  return (\n    <div>\n      <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 10 }}>\n        {COLORS.map((c) => (\n          <button\n            key={c}\n            onClick={() => setColor(c)}\n            aria-label={c}\n            style={{ width: 26, height: 26, borderRadius: '50%', background: c, border: color === c ? '3px solid #111' : '1px solid #ccc', cursor: 'pointer' }}\n          />\n        ))}\n        <label style={{ fontSize: 13 }}>\n          Size {size}\n          <input type=\"range\" min=\"1\" max=\"24\" value={size} onChange={(e) => setSize(Number(e.target.value))} />\n        </label>\n        <button onClick={clear} style={{ padding: '6px 12px', cursor: 'pointer' }}>Clear</button>\n      </div>\n      <canvas\n        ref={canvasRef}\n        width={420}\n        height={280}\n        onPointerDown={start}\n        onPointerMove={move}\n        onPointerUp={end}\n        onPointerLeave={end}\n        style={{ border: '1px solid #cbd5e1', borderRadius: 8, touchAction: 'none', cursor: 'crosshair', background: '#fff' }}\n      />\n    </div>\n  );\n}\n"
    }
  },
  "vue": {
    "answer": "## Building a Canvas Drawing Board in Vue 3\n\n**Mental model:** Grab the 2D context from a template-ref canvas. Pointer events drive a tiny state machine: `pointerdown` starts a stroke, `pointermove` while drawing extends it, `pointerup`/`pointerleave` end it. Brush colour/size are context properties set before stroking. Convert client → canvas coords with `getBoundingClientRect`.\n\n### Step 1 — Context from template ref\n```js\nconst canvas = ref(null);\nconst ctx = () => canvas.value.getContext('2d');\n```\n\n### Step 2 — Start / draw / end\n```js\nfunction start(e) { drawing = true; const c = ctx(); c.beginPath(); const [x, y] = pos(e); c.moveTo(x, y); }\nfunction move(e) { if (!drawing) return; const c = ctx(); c.strokeStyle = color.value; c.lineWidth = size.value; c.lineCap = 'round'; const [x, y] = pos(e); c.lineTo(x, y); c.stroke(); }\n```\n\n### Step 3 — Coord mapping\n```js\nfunction pos(e) { const r = canvas.value.getBoundingClientRect(); return [e.clientX - r.left, e.clientY - r.top]; }\n```\n\n### Step 4 — Clear\n```js\nfunction clear() { const cv = canvas.value; cv.getContext('2d').clearRect(0, 0, cv.width, cv.height); }\n```\n\n**Edge cases**\n\n| Case | Handling |\n| :-- | :-- |\n| Pointer leaves | End on `@pointerleave`. |\n| HiDPI | Scale by `devicePixelRatio`. |\n| Touch | Pointer events cover it; set `touch-action: none`. |\n\n**Interview tip:** Pointer events unify mouse/touch, and `touch-action: none` on the canvas stops the page scrolling while you draw on mobile — an easy detail to forget.\n",
    "files": {
      "/src/App.vue": "<script setup>\nimport DrawingBoard from './components/DrawingBoard.vue';\n</script>\n\n<template>\n  <div class=\"demo\">\n    <h1>Drawing Board Demo</h1>\n    <DrawingBoard />\n  </div>\n</template>\n\n<style>\n.demo {\n  font-family: sans-serif;\n  padding: 20px;\n}\n</style>\n",
      "/src/components/DrawingBoard.vue": "<script setup>\nimport { ref } from 'vue';\n\nconst COLORS = ['#111827', '#dc2626', '#16a34a', '#2563eb', '#d97706'];\n\nconst canvas = ref(null);\nconst color = ref('#111827');\nconst size = ref(4);\nlet drawing = false;\n\nfunction ctx() {\n  return canvas.value.getContext('2d');\n}\n\nfunction pos(e) {\n  const r = canvas.value.getBoundingClientRect();\n  return [e.clientX - r.left, e.clientY - r.top];\n}\n\nfunction start(e) {\n  drawing = true;\n  const c = ctx();\n  const [x, y] = pos(e);\n  c.beginPath();\n  c.moveTo(x, y);\n}\n\nfunction move(e) {\n  if (!drawing) return;\n  const c = ctx();\n  c.strokeStyle = color.value;\n  c.lineWidth = size.value;\n  c.lineCap = 'round';\n  c.lineJoin = 'round';\n  const [x, y] = pos(e);\n  c.lineTo(x, y);\n  c.stroke();\n}\n\nfunction end() {\n  drawing = false;\n}\n\nfunction clear() {\n  const cv = canvas.value;\n  cv.getContext('2d').clearRect(0, 0, cv.width, cv.height);\n}\n</script>\n\n<template>\n  <div>\n    <div class=\"toolbar\">\n      <button\n        v-for=\"c in COLORS\"\n        :key=\"c\"\n        class=\"swatch\"\n        :style=\"{ background: c, border: color === c ? '3px solid #111' : '1px solid #ccc' }\"\n        :aria-label=\"c\"\n        @click=\"color = c\"\n      ></button>\n      <label class=\"size\">Size {{ size }}<input type=\"range\" min=\"1\" max=\"24\" v-model.number=\"size\" /></label>\n      <button class=\"clear\" @click=\"clear\">Clear</button>\n    </div>\n    <canvas\n      ref=\"canvas\"\n      width=\"420\"\n      height=\"280\"\n      class=\"board\"\n      @pointerdown=\"start\"\n      @pointermove=\"move\"\n      @pointerup=\"end\"\n      @pointerleave=\"end\"\n    ></canvas>\n  </div>\n</template>\n\n<style scoped>\n.toolbar {\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  margin-bottom: 10px;\n}\n.swatch {\n  width: 26px;\n  height: 26px;\n  border-radius: 50%;\n  cursor: pointer;\n}\n.size {\n  font-size: 13px;\n}\n.clear {\n  padding: 6px 12px;\n  cursor: pointer;\n}\n.board {\n  border: 1px solid #cbd5e1;\n  border-radius: 8px;\n  touch-action: none;\n  cursor: crosshair;\n  background: #fff;\n}\n</style>\n"
    }
  },
  "angular": {
    "answer": "## Building a Canvas Drawing Board in Angular\n\n**Mental model:** Grab the 2D context from a `@ViewChild` canvas. Pointer events drive a tiny state machine: `(pointerdown)` starts a stroke, `(pointermove)` while drawing extends it, `(pointerup)`/`(pointerleave)` end it. Brush colour/size are context properties set before stroking. Map client → canvas coords with `getBoundingClientRect`. Classic `NgModule` setup, no `?.`/`??` (the build rejects them).\n\n### Step 1 — Context from ViewChild\n```typescript\n@ViewChild('cv') cv!: ElementRef<HTMLCanvasElement>;\nctx(): CanvasRenderingContext2D { return this.cv.nativeElement.getContext('2d') as CanvasRenderingContext2D; }\n```\n\n### Step 2 — Start / draw / end\n```typescript\nstart(e: PointerEvent): void { this.drawing = true; const c = this.ctx(); const p = this.pos(e); c.beginPath(); c.moveTo(p[0], p[1]); }\nmove(e: PointerEvent): void { if (!this.drawing) { return; } const c = this.ctx(); c.strokeStyle = this.color; c.lineWidth = this.size; c.lineCap = 'round'; const p = this.pos(e); c.lineTo(p[0], p[1]); c.stroke(); }\n```\n\n### Step 3 — Coord mapping\n```typescript\npos(e: PointerEvent): number[] { const r = this.cv.nativeElement.getBoundingClientRect(); return [e.clientX - r.left, e.clientY - r.top]; }\n```\n\n### Step 4 — Clear\n```typescript\nclear(): void { const cv = this.cv.nativeElement; this.ctx().clearRect(0, 0, cv.width, cv.height); }\n```\n\n**Edge cases**\n\n| Case | Handling |\n| :-- | :-- |\n| Pointer leaves | End on `(pointerleave)`. |\n| HiDPI | Scale by `devicePixelRatio`. |\n| Touch scroll | `touch-action: none` on the canvas. |\n\n**Interview tip:** Pointer events unify mouse + touch; `touch-action: none` prevents the page scrolling while drawing on mobile — and casting `getContext('2d')` to the non-null type keeps strict TS happy.\n",
    "files": {
      "/src/app/app.component.ts": "import { Component, ViewChild, ElementRef } from '@angular/core';\nimport { NgModule } from '@angular/core';\nimport { BrowserModule } from '@angular/platform-browser';\n\n@Component({\n  selector: 'app-drawing-board',\n  template: `\n    <div>\n      <div style=\"display: flex; align-items: center; gap: 12px; margin-bottom: 10px;\">\n        <button\n          *ngFor=\"let c of colors\"\n          (click)=\"color = c\"\n          [attr.aria-label]=\"c\"\n          [style.background]=\"c\"\n          [style.border]=\"color === c ? '3px solid #111' : '1px solid #ccc'\"\n          style=\"width: 26px; height: 26px; border-radius: 50%; cursor: pointer;\"\n        ></button>\n        <label style=\"font-size: 13px;\">Size {{ size }}\n          <input type=\"range\" min=\"1\" max=\"24\" [value]=\"size\" (input)=\"size = toNum($any($event.target).value)\" />\n        </label>\n        <button (click)=\"clear()\" style=\"padding: 6px 12px; cursor: pointer;\">Clear</button>\n      </div>\n      <canvas\n        #cv\n        width=\"420\"\n        height=\"280\"\n        (pointerdown)=\"start($event)\"\n        (pointermove)=\"move($event)\"\n        (pointerup)=\"end()\"\n        (pointerleave)=\"end()\"\n        style=\"border: 1px solid #cbd5e1; border-radius: 8px; touch-action: none; cursor: crosshair; background: #fff;\"\n      ></canvas>\n    </div>\n  `,\n})\nexport class DrawingBoardComponent {\n  @ViewChild('cv') cv!: ElementRef<HTMLCanvasElement>;\n  colors = ['#111827', '#dc2626', '#16a34a', '#2563eb', '#d97706'];\n  color = '#111827';\n  size = 4;\n  private drawing = false;\n\n  toNum(v: string): number {\n    return Number(v);\n  }\n\n  ctx(): CanvasRenderingContext2D {\n    return this.cv.nativeElement.getContext('2d') as CanvasRenderingContext2D;\n  }\n\n  pos(e: PointerEvent): number[] {\n    const r = this.cv.nativeElement.getBoundingClientRect();\n    return [e.clientX - r.left, e.clientY - r.top];\n  }\n\n  start(e: PointerEvent): void {\n    this.drawing = true;\n    const c = this.ctx();\n    const p = this.pos(e);\n    c.beginPath();\n    c.moveTo(p[0], p[1]);\n  }\n\n  move(e: PointerEvent): void {\n    if (!this.drawing) {\n      return;\n    }\n    const c = this.ctx();\n    c.strokeStyle = this.color;\n    c.lineWidth = this.size;\n    c.lineCap = 'round';\n    c.lineJoin = 'round';\n    const p = this.pos(e);\n    c.lineTo(p[0], p[1]);\n    c.stroke();\n  }\n\n  end(): void {\n    this.drawing = false;\n  }\n\n  clear(): void {\n    const cv = this.cv.nativeElement;\n    this.ctx().clearRect(0, 0, cv.width, cv.height);\n  }\n}\n\n@Component({\n  selector: 'app-root',\n  template: `\n    <div style=\"font-family: sans-serif; padding: 20px;\">\n      <h1>Drawing Board Demo</h1>\n      <app-drawing-board></app-drawing-board>\n    </div>\n  `,\n})\nexport class AppComponent {}\n\n@NgModule({\n  declarations: [AppComponent, DrawingBoardComponent],\n  imports: [BrowserModule],\n  bootstrap: [AppComponent],\n})\nexport class AppModule {}\n"
    }
  }
}