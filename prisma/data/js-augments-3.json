[
  {
    "title": "What are closures in JavaScript?",
    "answer": "**Core concept (TL;DR).** A *closure* is a function bundled together with references to the variables of the scope in which it was **defined** — its lexical environment. The inner function keeps a *live link* to those outer variables even after the outer function has already returned.\n\n<div style=\"margin:1.25rem auto;max-width:560px;border:1px solid rgba(245,158,11,0.25);border-radius:14px;padding:14px;background:rgba(245,158,11,0.05)\">\n<svg viewBox=\"0 0 520 210\" style=\"width:100%;display:block\" xmlns=\"http://www.w3.org/2000/svg\" font-family=\"ui-sans-serif,system-ui\">\n  <text x=\"16\" y=\"22\" fill=\"currentColor\" font-size=\"12\" font-weight=\"700\">A closure keeps its birth scope alive on the heap</text>\n  <rect x=\"20\" y=\"38\" width=\"300\" height=\"156\" rx=\"10\" fill=\"#f59e0b\" fill-opacity=\"0.08\" stroke=\"#f59e0b\" stroke-width=\"1.5\"/>\n  <text x=\"34\" y=\"58\" fill=\"#f59e0b\" font-size=\"11\" font-weight=\"700\">makeCounter() scope — survives return</text>\n  <rect x=\"40\" y=\"68\" width=\"150\" height=\"30\" rx=\"6\" fill=\"#f59e0b\" fill-opacity=\"0.18\" stroke=\"#f59e0b\"/>\n  <text x=\"115\" y=\"87\" fill=\"currentColor\" font-size=\"11\" text-anchor=\"middle\">let count = 0</text>\n  <rect x=\"40\" y=\"120\" width=\"262\" height=\"58\" rx=\"8\" fill=\"#3b82f6\" fill-opacity=\"0.10\" stroke=\"#3b82f6\" stroke-width=\"1.5\"/>\n  <text x=\"54\" y=\"140\" fill=\"#3b82f6\" font-size=\"11\" font-weight=\"700\">returned { inc, dec }</text>\n  <text x=\"54\" y=\"160\" fill=\"currentColor\" font-size=\"10\">reads &amp; writes ++count / --count</text>\n  <path d=\"M 120 120 C 120 108, 118 104, 116 100\" fill=\"none\" stroke=\"#ef4444\" stroke-width=\"2\" marker-end=\"url(#cl-a)\"/>\n  <text x=\"196\" y=\"115\" fill=\"#ef4444\" font-size=\"9.5\" font-weight=\"700\">live reference</text>\n  <rect x=\"340\" y=\"74\" width=\"160\" height=\"92\" rx=\"9\" fill=\"#64748b\" fill-opacity=\"0.08\" stroke=\"#64748b\" stroke-width=\"1.2\" stroke-dasharray=\"4 3\"/>\n  <text x=\"420\" y=\"96\" fill=\"currentColor\" font-size=\"10\" text-anchor=\"middle\" opacity=\"0.85\">Outside world</text>\n  <text x=\"420\" y=\"116\" fill=\"currentColor\" font-size=\"10\" text-anchor=\"middle\">const c = makeCounter()</text>\n  <text x=\"420\" y=\"138\" fill=\"currentColor\" font-size=\"10\" text-anchor=\"middle\">c.inc() → 1, 2, 3…</text>\n  <text x=\"420\" y=\"156\" fill=\"#ef4444\" font-size=\"9.5\" text-anchor=\"middle\">c.count → undefined</text>\n  <defs><marker id=\"cl-a\" markerWidth=\"7\" markerHeight=\"7\" refX=\"4\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L7,3 L0,6 Z\" fill=\"#ef4444\"/></marker></defs>\n</svg>\n</div>\n\n**How it works.** When a function is created, the engine attaches a hidden reference to the scope it was born in. Variable lookups walk *outward* along this chain. Because the link is to the **variable itself** (not a copy of its value), every call sees the latest value, and the scope is kept on the heap for as long as the inner function is reachable — which is why state outlives the outer call.\n\n### Where closures show up\n| Pattern | What the closure captures |\n| --- | --- |\n| Data privacy / module pattern | private state no caller can reach |\n| Function factories | configuration baked into the returned fn |\n| Memoization / caching | a cache that persists across calls |\n| Event handlers & callbacks | the surrounding variables at definition time |\n\n> **Interview tip:** Say explicitly that a closure captures a *reference to the variable*, not a snapshot of its value. That one sentence explains the classic <code>var</code>-in-a-<code>for</code>-loop bug (every callback logs the final value) and why a per-iteration <code>let</code> binding fixes it.",
    "examples": [
      {
        "label": "Private state + the loop-capture gotcha",
        "runnable": true,
        "code": "function makeCounter() {\n  let count = 0;               // private state, lives in the closure\n  return {\n    inc() { return ++count; },\n    dec() { return --count; },\n  };\n}\nconst c = makeCounter();\nconsole.log(c.inc(), c.inc(), c.dec()); // 1 2 1\nconsole.log(\"count is private:\", c.count); // undefined\n\n// var shares ONE binding; let creates a fresh binding each iteration\nconst withVar = [];\nfor (var i = 0; i < 3; i++) withVar.push(() => i);\nconsole.log(\"var:\", withVar.map(f => f())); // [3, 3, 3]\n\nconst withLet = [];\nfor (let j = 0; j < 3; j++) withLet.push(() => j);\nconsole.log(\"let:\", withLet.map(f => f())); // [0, 1, 2]"
      }
    ]
  },
  {
    "title": "What are computed property names?",
    "answer": "**Computed property names** let you use an expression as an object key in a literal: `{ [expr]: value }`. The expression is evaluated and its result becomes the key.\n\nThey're handy for dynamic keys, building objects from variables, and using symbols as keys.\n\n**Interview tip:** note they work in object literals, class members, and destructuring patterns.",
    "examples": [
      {
        "label": "dynamic keys",
        "code": "const key = 'role';\nconst id = 7;\nconst obj = { [key]: 'admin', [`user_${id}`]: true };\nconsole.log(obj); // { role: 'admin', user_7: true }"
      }
    ]
  },
  {
    "title": "What are modules in JavaScript?",
    "answer": "**Modules** split code into files with explicit `import`/`export`, each with its own scope (no global pollution). **ES Modules** (ESM) are the standard — static, tree-shakeable, with top-level `await`; **CommonJS** (`require`/`module.exports`) is Node's older synchronous system.\n\n**Interview tip:** mention named vs default exports, that ESM is static (analyzable at build time), and dynamic `import()` for lazy loading.",
    "examples": [
      {
        "label": "ES module syntax",
        "runnable": false,
        "code": "// math.js\nexport const add = (a, b) => a + b;\nexport default function mul(a, b) { return a * b; }\n\n// app.js\nimport mul, { add } from './math.js';\nconsole.log(add(2, 3), mul(2, 3)); // 5 6\n\n// lazy / dynamic import\nconst { add: add2 } = await import('./math.js');"
      }
    ]
  },
  {
    "title": "What are private class fields?",
    "answer": "Fields prefixed with **`#`** are **truly private** — accessible only inside the class body, enforced by the engine (not a naming convention like `_x`). Private methods and static private members are also supported.\n\nAccessing a `#field` from outside is a **syntax error**, and `#x in obj` can test for brand membership.\n\n**Interview tip:** contrast with the old `_underscore` convention (only soft privacy) and with closures-based privacy.",
    "examples": [
      {
        "label": "real privacy",
        "code": "class Account {\n  #balance = 0;\n  deposit(n) { this.#balance += n; return this; }\n  get balance() { return this.#balance; }\n}\nconst a = new Account();\na.deposit(100).deposit(50);\nconsole.log(a.balance);          // 150\nconsole.log(Object.keys(a));     // [] (#balance hidden)"
      }
    ]
  },
  {
    "title": "What are static methods and properties?",
    "answer": "**`static`** members belong to the **class itself**, not instances, and are accessed as `ClassName.member`. They're used for factory methods, constants, and utilities related to the class but not to a specific instance.\n\nStatic methods can't access instance `this`; `this` inside a static method refers to the class.\n\n**Interview tip:** common examples are `Array.from`, `Object.keys`, `Math.max`, and custom factory constructors.",
    "examples": [
      {
        "label": "factory + counter",
        "code": "class User {\n  static count = 0;\n  constructor(name) { this.name = name; User.count++; }\n  static of(name) { return new User(name); }   // factory\n}\nUser.of('Ada'); new User('Lin');\nconsole.log(User.count);   // 2\nconsole.log(User.of('Bo').name); // 'Bo'"
      }
    ]
  },
  {
    "title": "What are tagged template literals?",
    "answer": "A **tagged template** calls a function with a template literal's parts: the array of string chunks plus the interpolated values. The tag can transform or escape the output instead of just concatenating.\n\nUses: safe HTML/SQL escaping, i18n, and DSLs like styled-components and GraphQL's `gql`.\n\n**Interview tip:** know the tag signature `tag(strings, ...values)` and that `strings.raw` gives un-escaped text.",
    "examples": [
      {
        "label": "a custom tag",
        "code": "function upper(strings, ...values) {\n  return strings.reduce((out, s, i) =>\n    out + s + (i < values.length ? String(values[i]).toUpperCase() : ''), '');\n}\nconst name = 'ada';\nconsole.log(upper`hello ${name}, welcome`); // 'hello ADA, welcome'"
      }
    ]
  },
  {
    "title": "What are template literals?",
    "answer": "**Template literals** use backticks and support **interpolation** `${expr}`, **multi-line** strings, and **tagged** templates. They replace clumsy string concatenation.\n\n**Interview tip:** mention they can embed any expression, span lines without `\\n`, and that tagged templates enable safe escaping/DSLs.",
    "examples": [
      {
        "label": "interpolation + multiline",
        "code": "const user = { name: 'Ada', score: 95 };\nconst msg = `Hi ${user.name},\nyour score is ${user.score} (${user.score >= 90 ? 'A' : 'B'}).`;\nconsole.log(msg);"
      }
    ]
  },
  {
    "title": "What are the basics of regular expressions in JavaScript?",
    "answer": "A **RegExp** (`/pattern/flags`) matches text patterns. Common flags: `g` (global), `i` (case-insensitive), `m` (multiline), `s` (dotall). Use `test()` for a boolean; on strings, `match`/`matchAll`/`replace`/`split`.\n\n**Capture groups** `()` extract parts; named groups `(?<name>...)`; character classes `[a-z]`, quantifiers `*`/`+`/`{n,m}`, anchors `^`/`$`.\n\n**Interview tip:** mention greedy vs lazy quantifiers and backreferences as common follow-ups.",
    "examples": [
      {
        "label": "test, capture, replace",
        "code": "const re = /(\\d{4})-(\\d{2})-(\\d{2})/;\nconsole.log(re.test('2026-06-15'));        // true\nconst [, y, m, d] = '2026-06-15'.match(re);\nconsole.log(y, m, d);                      // 2026 06 15\nconsole.log('a1b2c3'.replace(/\\d/g, '#')); // 'a#b#c#'"
      }
    ]
  },
  {
    "title": "What are the quirks of the typeof operator?",
    "answer": "`typeof` returns a type string but has well-known quirks: `typeof null` is `'object'` (a historic bug), `typeof []` and `typeof {}` are both `'object'` (use `Array.isArray`), `typeof function(){}` is `'function'`, and `typeof NaN` is `'number'`. Crucially, `typeof undeclaredVar` safely returns `'undefined'` (no error).\n\n**Interview tip:** use `Array.isArray`, `value === null`, and `Number.isNaN` rather than relying on `typeof` for those cases.",
    "examples": [
      {
        "label": "the surprising ones",
        "code": "console.log(typeof null);       // 'object' (quirk)\nconsole.log(typeof []);         // 'object'\nconsole.log(typeof function(){}); // 'function'\nconsole.log(typeof NaN);        // 'number'\nconsole.log(typeof nope);       // 'undefined' (no ReferenceError)"
      }
    ]
  },
  {
    "title": "What do Array.from and Array.of do?",
    "answer": "**`Array.from(src, mapFn?)`** builds a real array from anything **iterable or array-like** (NodeLists, strings, Sets, `arguments`), optionally mapping each item. **`Array.of(...items)`** creates an array from its arguments — avoiding the `new Array(3)` ambiguity (which makes a length-3 empty array, not `[3]`).\n\n**Interview tip:** `Array.from({length: n}, (_, i) => i)` is a neat way to build a numeric range.",
    "examples": [
      {
        "label": "from iterables + ranges",
        "code": "console.log(Array.from('abc'));            // ['a','b','c']\nconsole.log(Array.from(new Set([1,1,2]))); // [1,2]\nconsole.log(Array.from({length: 4}, (_, i) => i * i)); // [0,1,4,9]\nconsole.log(Array.of(3));                  // [3]\nconsole.log(new Array(3).length);          // 3 (empty!)"
      }
    ]
  },
  {
    "title": "What do Object.keys, values, and entries return?",
    "answer": "All operate on an object's **own enumerable** string-keyed properties: **`keys`** → array of keys, **`values`** → array of values, **`entries`** → array of `[key, value]` pairs. `Object.fromEntries` reverses entries back into an object.\n\n**Interview tip:** `entries` pairs nicely with `for...of`/`map`, and `keys`/`values` exclude inherited and symbol keys (unlike `for...in`).",
    "examples": [
      {
        "label": "iterate + transform",
        "code": "const scores = { ada: 95, lin: 88 };\nconsole.log(Object.keys(scores));    // ['ada','lin']\nconsole.log(Object.values(scores));  // [95, 88]\nfor (const [k, v] of Object.entries(scores)) console.log(k, '=>', v);\nconst doubled = Object.fromEntries(\n  Object.entries(scores).map(([k, v]) => [k, v * 2]));\nconsole.log(doubled);                // { ada:190, lin:176 }"
      }
    ]
  },
  {
    "title": "What do flat and flatMap do?",
    "answer": "**`flat(depth = 1)`** flattens nested arrays by the given depth (`Infinity` for fully flat). **`flatMap(fn)`** maps each element then flattens the result one level — efficient for expanding each item into zero or more values.\n\n**Interview tip:** `flatMap` is `map().flat(1)` in one pass; returning `[]` from the callback drops an item, returning multiple expands it.",
    "examples": [
      {
        "label": "flatten + expand",
        "code": "console.log([1, [2, [3, [4]]]].flat(2)); // [1,2,3,[4]]\nconsole.log([1, [2, [3]]].flat(Infinity)); // [1,2,3]\n\nconst words = ['hi there', 'a b c'];\nconsole.log(words.flatMap(s => s.split(' '))); // ['hi','there','a','b','c']\nconsole.log([1,2,3].flatMap(n => n % 2 ? [n] : [])); // [1,3] (filter+map)"
      }
    ]
  },
  {
    "title": "What does 'use strict' do?",
    "answer": "**Strict mode** opts into a safer JavaScript variant. It throws on assignments to undeclared variables, makes `this` `undefined` in plain function calls (instead of the global object), forbids duplicate parameter names and `with`, and disallows writes to read-only/non-writable properties.\n\nES **modules** and **class** bodies are strict automatically.\n\n**Interview tip:** it converts many silent mistakes into thrown errors — catching bugs earlier.",
    "examples": [
      {
        "label": "silent error becomes thrown",
        "code": "function sloppy() { undeclared = 1; return 'ok'; }      // would leak a global\nfunction strict() { 'use strict'; try { undeclared2 = 1; } catch (e) { return e.name; } }\nconsole.log(strict()); // 'ReferenceError' (strict catches it)"
      }
    ]
  },
  {
    "title": "What does Array.filter do?",
    "answer": "**`filter(predicate)`** returns a **new array** with only the elements for which the predicate returns truthy. It doesn't mutate the source and is chainable with `map`/`reduce`.\n\n**Interview tip:** it always returns an array (possibly empty); use `find` when you only need the first match.",
    "examples": [
      {
        "label": "keep matching items",
        "code": "const nums = [1, 2, 3, 4, 5, 6];\nconsole.log(nums.filter(n => n % 2 === 0)); // [2,4,6]\nconsole.log(nums.filter(n => n > 10));      // []\nconst users = [{n:'A',active:true},{n:'B',active:false}];\nconsole.log(users.filter(u => u.active).map(u => u.n)); // ['A']"
      }
    ]
  },
  {
    "title": "What does Object.create do?",
    "answer": "**`Object.create(proto, props?)`** creates a new object with `proto` as its prototype (and optional property descriptors). It's the most direct way to set up prototypal inheritance/delegation.\n\n`Object.create(null)` makes a **prototype-less** object — a clean dictionary with no inherited keys like `toString`, useful as a safe map.\n\n**Interview tip:** contrast it with `new` (which also runs a constructor) and with class syntax.",
    "examples": [
      {
        "label": "delegation + null proto",
        "code": "const proto = { greet() { return 'hi ' + this.name; } };\nconst o = Object.create(proto);\no.name = 'Ada';\nconsole.log(o.greet());                 // 'hi Ada'\n\nconst dict = Object.create(null);\ndict.key = 1;\nconsole.log('toString' in dict);        // false (no prototype)"
      }
    ]
  },
  {
    "title": "What does setTimeout with 0 delay actually do?",
    "answer": "`setTimeout(fn, 0)` does **not** run `fn` immediately — it queues `fn` as a **macrotask** to run after the current synchronous code finishes *and* after all pending **microtasks** (promise callbacks) drain. Browsers also clamp nested timers to a ~4ms minimum.\n\n**Interview tip:** it's used to 'yield to the event loop' / defer work to the next tick, and it always runs after a resolved promise's `.then`.",
    "examples": [
      {
        "label": "deferred, after microtasks",
        "code": "console.log('start');\nsetTimeout(() => console.log('timeout (macrotask)'), 0);\nPromise.resolve().then(() => console.log('promise (microtask)'));\nconsole.log('end');\n// start, end, promise, timeout"
      }
    ]
  },
  {
    "title": "What does the delete operator do?",
    "answer": "**`delete obj.prop`** removes an own property from an object and returns `true`. On arrays it removes the element but **leaves a hole** (doesn't reindex or change `length`) — use `splice` instead. It can't delete variables or non-configurable properties.\n\n**Interview tip:** mention that deleting can deoptimize objects, so setting to `undefined` is sometimes preferred, and `delete` on arrays is a common bug.",
    "examples": [
      {
        "label": "objects vs arrays",
        "code": "const obj = { a: 1, b: 2 };\ndelete obj.a;\nconsole.log(obj);            // { b: 2 }\n\nconst arr = [1, 2, 3];\ndelete arr[1];\nconsole.log(arr, arr.length); // [1, <empty>, 3] 3  (hole!)"
      }
    ]
  },
  {
    "title": "What does yield* do in a generator?",
    "answer": "**`yield*`** delegates iteration to another iterable/generator, yielding all of its values as if they were inline. It also forwards the inner generator's return value.\n\nIt's used to **compose** generators — flattening, chaining, or building one sequence from sub-sequences.\n\n**Interview tip:** contrast `yield x` (yields one value) with `yield* iterable` (yields each value of the iterable).",
    "examples": [
      {
        "label": "compose generators",
        "code": "function* letters() { yield 'a'; yield 'b'; }\nfunction* nums() { yield 1; yield 2; }\nfunction* both() {\n  yield* letters();\n  yield* nums();\n  yield 'done';\n}\nconsole.log([...both()]); // ['a','b',1,2,'done']"
      }
    ]
  },
  {
    "title": "What is BigInt?",
    "answer": "**BigInt** represents integers of arbitrary size, beyond `Number.MAX_SAFE_INTEGER` (2^53−1). Write a literal with an `n` suffix (`123n`) or `BigInt(x)`.\n\nYou **can't mix** BigInt and Number in arithmetic without explicit conversion, and BigInt doesn't support fractions. Useful for large IDs, timestamps, and precise integer math (e.g., cryptography).\n\n**Interview tip:** mention the safe-integer limit and why floats lose precision for big integers.",
    "examples": [
      {
        "label": "beyond safe integers",
        "code": "console.log(Number.MAX_SAFE_INTEGER);       // 9007199254740991\nconsole.log(9007199254740993 === 9007199254740992); // true (precision lost!)\nconst big = 9007199254740993n;\nconsole.log(big + 1n);                       // 9007199254740994n (exact)\nconsole.log(typeof big);                     // 'bigint'"
      }
    ]
  },
  {
    "title": "What is CORS?",
    "answer": "**CORS** (Cross-Origin Resource Sharing) is a browser security feature that blocks cross-origin requests unless the server opts in with `Access-Control-Allow-Origin` (and related) headers. 'Non-simple' requests trigger a **preflight** `OPTIONS` request first.\n\nIt's enforced by the **browser**, not the server — so server-to-server calls aren't subject to it.\n\n**Interview tip:** know it protects users (same-origin policy), the preflight mechanism, and that credentials require `Allow-Credentials` + a specific origin.",
    "examples": [
      {
        "label": "server opts in (response headers)",
        "runnable": false,
        "code": "// Server response that allows a cross-origin browser request:\n// Access-Control-Allow-Origin: https://app.example.com\n// Access-Control-Allow-Methods: GET, POST\n// Access-Control-Allow-Headers: Content-Type\n// Access-Control-Allow-Credentials: true\n\n// Browser preflight (automatic) for a non-simple request:\n// OPTIONS /api  ->  server must answer with the headers above"
      }
    ]
  },
  {
    "title": "What is JSON and why is it commonly used?",
    "answer": "**JSON** (JavaScript Object Notation) is a lightweight, language-independent text format for structured data — objects, arrays, strings, numbers, booleans, and `null`. It's human-readable and maps cleanly to most languages' data structures.\n\nIt's the de-facto format for APIs and config. In JS: `JSON.stringify` (serialize) and `JSON.parse` (deserialize), with optional replacer/reviver functions.\n\n**Interview tip:** note JSON limits — no functions, `undefined`, comments, or `Date` objects (dates become strings).",
    "examples": [
      {
        "label": "stringify / parse",
        "code": "const obj = { name: 'Ada', tags: ['js', 'cs'], active: true };\nconst json = JSON.stringify(obj);\nconsole.log(json);                  // '{\"name\":\"Ada\",...}'\nconst back = JSON.parse(json);\nconsole.log(back.tags[0]);          // 'js'\nconsole.log(JSON.stringify({ f: () => 1, u: undefined })); // '{}' (dropped)"
      }
    ]
  },
  {
    "title": "What is a higher-order function?",
    "answer": "**Core concept (TL;DR).** A *higher-order function* (HOF) is a function that does at least one of two things: it **takes another function as an argument**, or it **returns a function**. They are possible because functions in JavaScript are first-class values — you can pass them around like any other data.\n\n<div style=\"margin:1.25rem auto;max-width:560px;border:1px solid rgba(245,158,11,0.25);border-radius:14px;padding:14px;background:rgba(245,158,11,0.05)\">\n<svg viewBox=\"0 0 520 180\" style=\"width:100%;display:block\" xmlns=\"http://www.w3.org/2000/svg\" font-family=\"ui-sans-serif,system-ui\">\n  <text x=\"16\" y=\"22\" fill=\"currentColor\" font-size=\"12\" font-weight=\"700\">Functions in, function out</text>\n  <rect x=\"20\" y=\"64\" width=\"120\" height=\"44\" rx=\"8\" fill=\"#3b82f6\" fill-opacity=\"0.12\" stroke=\"#3b82f6\" stroke-width=\"1.5\"/>\n  <text x=\"80\" y=\"90\" fill=\"#3b82f6\" font-size=\"11\" font-weight=\"700\" text-anchor=\"middle\">callback fn</text>\n  <rect x=\"200\" y=\"50\" width=\"130\" height=\"72\" rx=\"10\" fill=\"#f59e0b\" fill-opacity=\"0.14\" stroke=\"#f59e0b\" stroke-width=\"1.6\"/>\n  <text x=\"265\" y=\"82\" fill=\"#f59e0b\" font-size=\"12\" font-weight=\"700\" text-anchor=\"middle\">HOF</text>\n  <text x=\"265\" y=\"102\" fill=\"currentColor\" font-size=\"9\" text-anchor=\"middle\">map / filter / compose</text>\n  <rect x=\"390\" y=\"64\" width=\"120\" height=\"44\" rx=\"8\" fill=\"#22c55e\" fill-opacity=\"0.12\" stroke=\"#22c55e\" stroke-width=\"1.5\"/>\n  <text x=\"450\" y=\"84\" fill=\"#22c55e\" font-size=\"11\" font-weight=\"700\" text-anchor=\"middle\">result or</text>\n  <text x=\"450\" y=\"100\" fill=\"#22c55e\" font-size=\"11\" font-weight=\"700\" text-anchor=\"middle\">new fn</text>\n  <path d=\"M 140 86 L 198 86\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.6\" marker-end=\"url(#hof-a)\"/>\n  <path d=\"M 330 86 L 388 86\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.6\" marker-end=\"url(#hof-a)\"/>\n  <defs><marker id=\"hof-a\" markerWidth=\"7\" markerHeight=\"7\" refX=\"4\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L7,3 L0,6 Z\" fill=\"currentColor\"/></marker></defs>\n</svg>\n</div>\n\n**How it works.** Because a function value can be stored, passed, and returned, you can build small composable behaviours. <code>Array.prototype.map</code>, <code>filter</code>, and <code>reduce</code> are HOFs that take a callback. Returning a function lets you **pre-bake configuration** — the basis of function factories, currying, decorators (logging/memoizing wrappers), and partial application.\n\n### Built-in HOFs you'll use daily\n| HOF | Takes | Returns |\n| --- | --- | --- |\n| <code>map</code> | transform fn | a new array |\n| <code>filter</code> | predicate fn | a new (shorter) array |\n| <code>reduce</code> | reducer fn | a single accumulated value |\n| <code>sort</code> | comparator fn | the sorted array |\n\n> **Interview tip:** Tie HOFs to \"functions are first-class citizens.\" A strong follow-up answer shows a function that *returns* a function (e.g. a <code>withLogging</code> wrapper or a <code>multiplier</code> factory), not just one that takes a callback.",
    "examples": [
      {
        "label": "Taking and returning functions",
        "runnable": true,
        "code": "const nums = [1, 2, 3, 4, 5];\n\n// HOFs that TAKE a function\nconst doubled = nums.map(n => n * 2);\nconst evens   = nums.filter(n => n % 2 === 0);\nconst sum     = nums.reduce((acc, n) => acc + n, 0);\nconsole.log(doubled, evens, sum);  // [2,4,6,8,10] [2,4] 15\n\n// A HOF that RETURNS a function (a decorator)\nfunction withLogging(fn) {\n  return function (...args) {\n    console.log(\"calling with\", args);\n    const out = fn(...args);\n    console.log(\"→ returned\", out);\n    return out;\n  };\n}\nconst add = withLogging((a, b) => a + b);\nadd(2, 3);  // logs the call, then \"→ returned 5\""
      }
    ]
  },
  {
    "title": "What is a pure function?",
    "answer": "**Core concept (TL;DR).** A *pure function* satisfies two rules: **(1)** given the same inputs it always returns the same output (deterministic), and **(2)** it produces **no side effects** — it doesn't mutate external state, touch the DOM, log, call an API, or read changing globals. Its output depends only on its arguments.\n\n<div style=\"margin:1.25rem auto;max-width:560px;border:1px solid rgba(245,158,11,0.25);border-radius:14px;padding:14px;background:rgba(245,158,11,0.05)\">\n<svg viewBox=\"0 0 520 188\" style=\"width:100%;display:block\" xmlns=\"http://www.w3.org/2000/svg\" font-family=\"ui-sans-serif,system-ui\">\n  <text x=\"16\" y=\"22\" fill=\"currentColor\" font-size=\"12\" font-weight=\"700\">Pure: isolated. Impure: reaches outside.</text>\n  <rect x=\"20\" y=\"40\" width=\"230\" height=\"138\" rx=\"10\" fill=\"#22c55e\" fill-opacity=\"0.08\" stroke=\"#22c55e\" stroke-width=\"1.5\"/>\n  <text x=\"135\" y=\"60\" fill=\"#22c55e\" font-size=\"11\" font-weight=\"700\" text-anchor=\"middle\">Pure</text>\n  <rect x=\"44\" y=\"92\" width=\"60\" height=\"32\" rx=\"6\" fill=\"#22c55e\" fill-opacity=\"0.18\" stroke=\"#22c55e\"/>\n  <text x=\"74\" y=\"112\" fill=\"currentColor\" font-size=\"9.5\" text-anchor=\"middle\">input</text>\n  <rect x=\"166\" y=\"92\" width=\"60\" height=\"32\" rx=\"6\" fill=\"#22c55e\" fill-opacity=\"0.18\" stroke=\"#22c55e\"/>\n  <text x=\"196\" y=\"112\" fill=\"currentColor\" font-size=\"9.5\" text-anchor=\"middle\">output</text>\n  <path d=\"M 104 108 L 164 108\" fill=\"none\" stroke=\"#22c55e\" stroke-width=\"1.8\" marker-end=\"url(#pf-a)\"/>\n  <text x=\"135\" y=\"152\" fill=\"#22c55e\" font-size=\"9\" text-anchor=\"middle\">same in → same out</text>\n  <rect x=\"270\" y=\"40\" width=\"230\" height=\"138\" rx=\"10\" fill=\"#ef4444\" fill-opacity=\"0.07\" stroke=\"#ef4444\" stroke-width=\"1.5\"/>\n  <text x=\"385\" y=\"60\" fill=\"#ef4444\" font-size=\"11\" font-weight=\"700\" text-anchor=\"middle\">Impure</text>\n  <rect x=\"300\" y=\"92\" width=\"64\" height=\"32\" rx=\"6\" fill=\"#ef4444\" fill-opacity=\"0.16\" stroke=\"#ef4444\"/>\n  <text x=\"332\" y=\"112\" fill=\"currentColor\" font-size=\"9.5\" text-anchor=\"middle\">fn()</text>\n  <path d=\"M 364 100 L 470 76\" fill=\"none\" stroke=\"#ef4444\" stroke-width=\"1.5\" marker-end=\"url(#pf-a)\"/>\n  <path d=\"M 364 116 L 470 142\" fill=\"none\" stroke=\"#ef4444\" stroke-width=\"1.5\" marker-end=\"url(#pf-a)\"/>\n  <text x=\"472\" y=\"74\" fill=\"#ef4444\" font-size=\"9\" text-anchor=\"end\">mutates global</text>\n  <text x=\"472\" y=\"150\" fill=\"#ef4444\" font-size=\"9\" text-anchor=\"end\">DOM / API / log</text>\n  <defs><marker id=\"pf-a\" markerWidth=\"7\" markerHeight=\"7\" refX=\"4\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L7,3 L0,6 Z\" fill=\"currentColor\"/></marker></defs>\n</svg>\n</div>\n\n**How it works.** Purity makes code **referentially transparent** — a call can be replaced by its result without changing behaviour. That is what makes pure functions trivial to test (no mocks, no setup), safe to cache/memoize, easy to run in parallel, and the reason React reducers and selectors are expected to be pure. Side-effecting work isn't bad — you just push it to the edges and keep the core logic pure.\n\n### Pure vs impure\n| | Pure | Impure |\n| --- | --- | --- |\n| Same input → same output | always | not guaranteed |\n| Mutates arguments / globals | never | often |\n| Side effects (DOM, I/O, log) | none | yes |\n| Testability | trivial | needs mocks/setup |\n\n> **Interview tip:** Call out that array methods split along this line — <code>map/filter/slice</code> are pure (return new arrays) while <code>push/splice/sort</code> mutate in place. Preferring the pure ones avoids a whole class of shared-state bugs.",
    "examples": [
      {
        "label": "Pure vs impure side by side",
        "runnable": true,
        "code": "// PURE — depends only on input, returns a new value\nfunction add(a, b) { return a + b; }\nfunction addItem(cart, item) { return [...cart, item]; } // no mutation\n\nconsole.log(add(2, 3));            // 3 → always 5... er, 5\nconsole.log(add(2, 3));            // 5 again (deterministic)\n\nconst cart = [\"apple\"];\nconsole.log(addItem(cart, \"pear\")); // [\"apple\",\"pear\"]\nconsole.log(cart);                  // [\"apple\"] — original untouched\n\n// IMPURE — mutates external state, output depends on it\nlet total = 0;\nfunction addToTotal(n) { total += n; return total; } // side effect\nconsole.log(addToTotal(5)); // 5\nconsole.log(addToTotal(5)); // 10 — same input, different output!"
      }
    ]
  },
  {
    "title": "What is an IIFE?",
    "answer": "An **IIFE** (Immediately Invoked Function Expression) is a function defined and called at once: `(function(){ ... })()` or `(() => { ... })()`. It creates a private scope, avoiding global pollution and name clashes.\n\nBefore ES modules and block scoping, IIFEs were the standard module pattern and a way to capture loop variables.\n\n**Interview tip:** mention it can return a public API (the module pattern) while keeping internals private.",
    "examples": [
      {
        "label": "private scope + module pattern",
        "code": "const counter = (function () {\n  let count = 0;                 // private\n  return { inc: () => ++count, value: () => count };\n})();\ncounter.inc(); counter.inc();\nconsole.log(counter.value());    // 2\nconsole.log(typeof count);       // 'undefined' (not leaked)"
      }
    ]
  },
  {
    "title": "What is debouncing and throttling?",
    "answer": "**Core Concept (TL;DR)**\nDebouncing and throttling are optimization techniques used to limit the execution rate of a function responding to high-frequency events. Debouncing delays execution until a quiet period occurs, while throttling limits execution to once per time interval.\n\n<div style=\"margin:1.25rem auto;max-width:540px;border:1px solid rgba(139,92,246,0.25);border-radius:14px;padding:14px;background:rgba(139,92,246,0.04)\">\n<svg viewBox=\"0 0 520 180\" style=\"width:100%;display:block\" xmlns=\"http://www.w3.org/2000/svg\" font-family=\"ui-sans-serif,system-ui\">\n  <text x=\"14\" y=\"22\" fill=\"currentColor\" font-size=\"12\" font-weight=\"700\">Debouncing waits for pause, Throttling limits rates</text>\n  <line x1=\"30\" y1=\"110\" x2=\"480\" y2=\"110\" stroke=\"currentColor\" stroke-width=\"2\" marker-end=\"url(#time-arrow)\"/>\n  <circle cx=\"60\" cy=\"110\" r=\"6\" fill=\"#ef4444\" />\n  <circle cx=\"100\" cy=\"110\" r=\"6\" fill=\"#ef4444\" />\n  <circle cx=\"140\" cy=\"110\" r=\"6\" fill=\"#ef4444\" />\n  <text x=\"100\" y=\"96\" fill=\"#ef4444\" font-size=\"9\" text-anchor=\"middle\">events fired</text>\n\n  \n  <rect x=\"140\" y=\"55\" width=\"120\" height=\"30\" rx=\"4\" fill=\"#3b82f6\" fill-opacity=\"0.15\" stroke=\"#3b82f6\" stroke-dasharray=\"4\"/>\n  <text x=\"200\" y=\"73\" fill=\"#3b82f6\" font-size=\"10\" text-anchor=\"middle\">debounce wait</text>\n  <circle cx=\"260\" cy=\"110\" r=\"8\" fill=\"#22c55e\"/>\n  <text x=\"260\" y=\"134\" fill=\"#22c55e\" font-size=\"10\" font-weight=\"700\" text-anchor=\"middle\">Execution</text>\n  \n  <defs>\n    <marker id=\"time-arrow\" markerWidth=\"6\" markerHeight=\"6\" refX=\"4\" refY=\"3\" orient=\"auto\">\n      <path d=\"M0,0 L6,3 L0,6 Z\" fill=\"currentColor\"/>\n    </marker>\n  </defs>\n</svg>\n</div>\n\n**Under-the-Hood Mechanics**\n- **Debounce**:\n  - Starts a timer on each event trigger.\n  - If a new event fires before the timer completes, the previous timer is cancelled, and a new one starts.\n  - The function only executes once the timer completes.\n  - Excellent for input validation, search inputs, and auto-save fields.\n- **Throttle**:\n  - Sets a block window once executed.\n  - Ignores or buffers intermediate event triggers until the block window expires.\n  - Guarantees execution at a regular interval.\n  - Excellent for scroll event trackers, resize layouts, and drag-and-drop handles.\n\n**Tricky Code Scenarios & Gotchas**\n- **Losing Closure State**:\n  Debounce/throttle wrappers rely on closures to maintain timer states (`timeoutId` or `lastRunTime`).\n  ```javascript\n  // Bug: Instantiating the wrapper inside a React functional component render loop creates a new closure on every render!\n  // Fix: Wrap the debounced function in a hook (e.g., useCallback or useRef) to persist the closure.\n  ```\n\n**Architectural Rationale**\nThese optimization strategies reduce execution pressure on CPU, memory, and networking pipelines, preventing application lag and eliminating server-side rate limit errors (like HTTP 429).\n\n**Pro-Level Interview Tip**\nIllustrate how debouncing helps search auto-suggest fields, and how throttling prevents performance lags on layout scroll event listeners.",
    "examples": [
      {
        "label": "debounce vs throttle behavior",
        "code": "function debounce(fn, ms) { let t; return (...a) => { clearTimeout(t); t = setTimeout(() => fn(...a), ms); }; }\nfunction throttle(fn, ms) { let last = 0; return (...a) => { const n = Date.now(); if (n - last >= ms) { last = n; fn(...a); } }; }\n\nconst d = debounce(() => console.log('debounced'), 50);\nconst t = throttle(() => console.log('throttled'), 50);\nd(); d(); d();   // 'debounced' once (after it goes quiet)\nt(); t(); t();   // 'throttled' once (immediately, rest ignored)"
      }
    ]
  }
]